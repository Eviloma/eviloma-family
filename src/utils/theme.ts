import { createTheme } from '@mantine/core';

const theme = createTheme({
  fontFamily: 'Raleway',
  primaryColor: 'violet',
  autoContrast: true,
  cursorType: 'pointer',
  defaultRadius: 'md',
});

export default theme;
